{
  "name": "dpu-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ploinky DPU MCP agent for confidential storage, secrets, and access control.",
  "scripts": {
    "test": "node --test ./tests/*.test.mjs"
  }
}
