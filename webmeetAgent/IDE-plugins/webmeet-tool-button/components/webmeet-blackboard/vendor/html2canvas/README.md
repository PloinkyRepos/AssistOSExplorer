# html2canvas vendor copy

This directory contains the local WebMeet browser bundle of `html2canvas` 1.4.1.
Blackboard group export uses its DOM renderer with `foreignObjectRendering: false`
so PNG output remains origin-clean and works without a runtime CDN dependency.

See `THIRD_PARTY_NOTICES.md` for provenance and integrity information.
