---
id: DS10
title: LiveKit Media Runtime
status: implemented
owner: webmeet-team
summary: Defines the WebMeet control/media/recording split, LiveKit and WebRTC runtime contracts, public media topology, recording and Redis boundaries, and scaling constraints.
---

# DS10 - LiveKit Media Runtime

## Introduction

This specification is the local WebMeet authority for the LiveKit media runtime. It folds the durable content that previously lived in the repository-level WebMeet architecture and WebRTC explainer documents into the `webmeetAgent` DS set so future changes have one spec location for WebMeet media contracts.

`webmeetAgent` owns WebMeet room state, token issuance, guest invite behavior, recording commands, AI artifacts, and the Explorer WebMeet plugin. It does not stream live audio, video, or screen-share packets. Live media is routed by the LiveKit server supplied by the `webmeetInfra` repo.

## Core Content

### Traffic Planes

WebMeet has separate planes with different owners and failure modes:

| Plane | Handles | Owning component |
|---|---|---|
| Control plane | Workspaces, rooms, members, chat, transcript, artifacts, AI jobs, recording commands, LiveKit participant tokens | `webmeetAgent` through Ploinky MCP tools and the scoped guest HTTP service |
| Media plane | Live microphone, camera, screen share, data-channel delivery, WebRTC transport negotiation, RTP/RTCP forwarding | LiveKit Server supervised by `liveKitServerAgent`, accessed through browser LiveKit clients |
| Recording plane | Room composite capture, rendering/compositing, MP4 encoding, file writes | LiveKit Egress supervised by `liveKitServerAgent` |
| LiveKit runtime state | LiveKit room/node state, message bus behavior, egress coordination | Redis supervised by `liveKitServerAgent` |
| Durable WebMeet state | Meeting records, encrypted payloads, chat, transcript, jobs, artifact metadata | `.ploinky/data/webmeetAgent/data` and `.ploinky/data/webmeet/recordings` |

The practical invariant is that MCP/API latency is not the live media path. After a browser joins LiveKit, live packets flow between the browser and LiveKit; `webmeetAgent` remains in the command and persistence path.

### Repository And Runtime Roles

The current Explorer WebMeet path is:

1. Starting `AchillesIDE/explorer` starts Explorer as the static agent.
2. Explorer enables `webmeetAgent` and its `webmeetInfra/liveKitServerAgent` dependency.
3. `webmeetAgent` exposes MCP tools and ships the WebMeet Explorer IDE plugin assets.
4. `webmeetInfra/liveKitServerAgent` runs Redis, LiveKit Server, LiveKit Egress, Coturn, and (in `prod`) Nginx + Certbot inside one supervised container.

The older Ploinky router-level `/webmeet` surface is not the current Explorer WebMeet application flow. The active UI path is `webmeetAgent/IDE-plugins/webmeet-tool-button`.

`webmeetAgent` joins the `webmeet` container network so it can resolve internal media services by alias. Browser application actions normally enter `webmeetAgent` through the router MCP proxy or the manifest-declared guest HTTP service, not through direct container ports.

### Infrastructure Agent

The single `webmeetInfra` agent is `liveKitServerAgent`, which supervises:

| Supervised service | Job | Notes |
|---|---|---|
| Redis | Runtime state for LiveKit and Egress | Infrastructure state, not the WebMeet durable meeting store. |
| Coturn | TURN/STUN relay | Returned to browsers as ICE servers only when TURN env vars are configured. |
| LiveKit Server | SFU, signaling, API, WebRTC transport | Generated config mounted at `/working-data/generated/livekit.yaml`. |
| LiveKit Egress | Recording worker | Mounts `.ploinky/data/webmeet/recordings` at `/data/recordings`. |
| Nginx + Certbot | TLS terminator and ACME renewal | `prod` profile only; supervised alongside the other services. |

The supervisor sequences startup as Redis → Coturn → LiveKit Server → LiveKit Egress, then (in `prod`) Nginx and the Certbot renewal loop, then opens the TCP health endpoint that Ploinky probes for readiness. Once the agent reports ready, Ploinky starts `webmeetAgent`. Explorer is the final consumer.

### Generated LiveKit And Egress Configuration

The checked-in LiveKit and egress YAML files in `webmeetInfra` are placeholders. Runtime config is generated by host preinstall hooks under `.ploinky/agents/...` before containers start.

`liveKitServerAgent`'s preinstall generates LiveKit config that includes:

- `port` for HTTP API and WebSocket signaling (`7880` in `default` and `prod`, `17880` in `dev`).
- `rtc.tcp_port` for TCP media fallback.
- `rtc.port_range_start` and `rtc.port_range_end` for UDP media sockets.
- `rtc.use_external_ip`, controlled by `WEBMEET_LIVEKIT_USE_EXTERNAL_IP`.
- Optional `rtc.node_ip`, controlled by `WEBMEET_LIVEKIT_NODE_IP` when external IP discovery is disabled.
- `rtc.force_tcp`, controlled by `WEBMEET_LIVEKIT_FORCE_TCP`.
- `logging.level`, controlled by `WEBMEET_LIVEKIT_LOG_LEVEL`.
- `redis.address`, controlled by `WEBMEET_LIVEKIT_REDIS_ADDRESS`. Because Redis runs as a supervised service inside the same container as LiveKit, this is `127.0.0.1:6379` in every profile.
- A LiveKit `keys` map whose API key and secret match the values `webmeetAgent` uses to sign participant tokens.

The runtime image is pinned through `WEBMEET_INFRA_IMAGE_TAG`, which the `liveKitServerAgent` manifest expands into `docker.io/assistos/livekit-server-agent:${WEBMEET_INFRA_IMAGE_TAG}`. Operators set the tag through the workspace var or the deploy-workflow input; the manifest must not reference `:latest`.

`liveKitServerAgent`'s network namespace is profile-specific (see `webmeetInfra/docs/specs/DS002`). The `prod` profile runs with `network.mode: "host"` and binds `7880/tcp`, `7881/tcp`, and `7882-7892/udp` directly on the host's network namespace. The `default` and `dev` profiles run on the shared `webmeet` bridge with alias `liveKitServerAgent`; `dev` uses the alternate range (`17880`, `17881`, `17882-17892/udp`) to avoid colliding with a default-profile run on the same workstation. Production bridge port-publishing for the LiveKit UDP range is avoided because podman's UDP src-NAT path rewrites client addresses to bridge-internal IPs and breaks the SFU's server-initiated downlink.

The supervised Egress process uses generated config with:

- `ws_url`, controlled by `WEBMEET_LIVEKIT_INTERNAL_WS_URL`. Since Egress and LiveKit run inside the same container, the default is `ws://127.0.0.1:7880` (`:17880` in `dev`).
- The same LiveKit API key/secret as the server.
- `redis.address`, controlled by `WEBMEET_EGRESS_REDIS_ADDRESS` (default `127.0.0.1:6379`).
- `health_port: 7980`.
- `insecure: true`, because Egress talks to LiveKit over loopback inside the container.

Egress is a media-processing service that must stay off untrusted networks. The current `liveKitServerAgent` manifest runs it without `containerSecurity.privileged: true`; if a future Egress release requires elevated privileges, set that flag explicitly in the manifest and update DS002.

### Room Discovery And Participant Rendezvous

Users do not discover WebMeet rooms through LiveKit or Redis. Room discovery is an application-plane operation owned by `webmeetAgent`.

For authenticated Explorer users:

1. The WebMeet plugin calls `webmeet_workspace_list`.
2. `webmeetAgent` derives the workspace id from the workspace root and ensures a workspace record.
3. The plugin calls `webmeet_meeting_list`.
4. `webmeetAgent` reads its mounted data directory (`/data/meetings/*.json`, backed by `.ploinky/data/webmeetAgent/data/meetings/*.json`), filters active rooms for the workspace, and returns room cards.
5. Admin users can create, rename, delete, and expose public meeting invite links. Non-admin users can see and join active rooms returned for the workspace.
6. Authenticated clients use the protected MCP-compatible workspace event list transport for outside-room state. A `meeting.created` event refreshes the room list so users who are already logged in see new rooms without reloading Explorer, and `meeting.renamed` updates room titles in place. Workspace-level participant and agent events refresh the room-card roster state for visible rooms so users can decide whether to join from current presence.

For guests:

1. An admin creates a public meeting and `webmeetAgent` stores a random `guestToken`.
2. The invite URL uses `/public-services/webmeet/guest?room=<meetingId>&token=<guestToken>`.
3. Ploinky routes that path through the manifest-declared guest HTTP service with `forceGuest: true`.
4. `webmeet-public-proxy.mjs` verifies the router-issued `__http_service__` invocation token before serving the guest page and guest APIs.
5. The guest join API verifies the room id and guest token before returning meeting state and a LiveKit participant token. The guest join page may remember the last guest display name in browser localStorage and prefill the name field on refresh or later visits from the same browser, but that convenience state is browser-local only and must not weaken invite-token checks or create durable server-side identity by itself.

After join there are two participant views:

- WebMeet presence is durable application state in the encrypted meeting payload, updated by join, heartbeat, leave, and stale-presence cleanup.
- Live room presence is LiveKit room state, surfaced to the UI through participant, publication, subscription, mute, unmute, active speaker, data-channel, participant-attribute, and disconnect events. Connected authenticated users and guests both use LiveKit for intra-room state; guest public HTTP APIs are limited to joining, leaving, and persisting allowed guest actions such as chat, not driving live participant cards.

The frontend is split by responsibility, even when the authenticated dashboard and guest page reuse the same visual components. The common room runtime owns selected-room behavior: LiveKit connect/disconnect, media controls, participant cards, chat delivery, avatar live sync, presence pings, and cleanup. The authenticated shell owns workspace discovery, room management, profile settings, and admin actions. The guest shell owns invite-token entry and bootstraps exactly one room session from the public link. Both shells must call the same runtime/API boundary for room actions: authenticated sessions use protected MCP-compatible WebMeet tools, while guest sessions use only the scoped public guest endpoints. New room features should be added to the common runtime first; shell-specific code should only choose the session source and allowed controls.

Participant cards resolve profile avatars without reading another user's DPU My Space. Local authenticated participants map to the current workspace user and may render the saved AxiFace profile avatar from Explorer's protected avatar settings service. WebMeet may also apply a browser-scoped full avatar override stored in localStorage for the current browser identity, including authenticated users and joined guests. The override is WebMeet-local state, does not write Explorer profile settings, and can replace the effective WebMeet avatar configuration for that browser/profile. The inline quick action is intentionally narrower: it only applies state/emotion presets to the browser-scoped WebMeet avatar. Effective avatar precedence is: browser-scoped WebMeet override, saved Explorer profile avatar when available, then first-letter fallback. Authenticated joins must persist the workspace user id in durable WebMeet presence and include the same id in LiveKit participant token attributes (`webmeetUserId`, `userId`, `workspaceUserId`, and `ploinkyUserId`) so other room members can identify the participant without guessing from display names. Authenticated WebMeet API calls must use the protected `/services/webmeet/...` router prefix. Forced-guest invite traffic uses `/public-services/webmeet/...`, where only the scoped guest actions are allowed. Guest WebMeet UI assets must not import protected Explorer avatar runtime modules directly; guest pages must load AxiFace modules and styles through the guest-scoped WebMeet public-service asset route (`/public-services/webmeet/axi-face/...`) so participant cards can render without protected Explorer access. A participant that has just completed the WebMeet join step but has not yet appeared in LiveKit must remain in durable meeting membership until either LiveKit exposes that identity or normal presence TTL cleanup removes it; room-detail reconciliation must not drop that pending member just because another viewer refreshes the meeting before the media-plane connect finishes. The client must not guess the room avatar from pre-join browser state alone. Instead, once the join response returns the authoritative participant identity and user mapping, the client resolves one effective avatar projection for that participant and propagates the same projection through LiveKit participant attributes plus the reliable data channel. Avatar projection is LiveKit-only room rendering state, not durable meeting state: WebMeet meeting records and meeting-detail refreshes must not persist, reconstruct, or serve room avatars from stored copies. The only authoritative avatar state for connected participant cards is the current LiveKit participant attribute/data-channel projection held by the connected clients. While a room is connected, meeting-detail loads may refresh only durable room data such as chat, transcript, artifacts, tasks, decisions, recordings, and agents; they must not overwrite the live participant list from snapshot payloads. Existing participants must republish their current avatar projection when a new room participant connects so late joiners receive the same live rendering state without forcing a meeting-store refresh. The already connected participant must also request current avatar projections at `participant connected` time so the newcomer's first live avatar publication is retried toward peers that were already in the room. Connect and reconnect flows must replay the participant's current live avatar projection from the already published runtime state before falling back to recomputing it from profile or override inputs. Avatar publishing is best-effort after the participant joins/connects; failure to publish the avatar must not block meeting join. After a participant finishes connecting, the client must also request current avatar projections from already connected peers over the reliable LiveKit data channel so any join-time attribute race is healed without touching meeting persistence. The participant-card controller must keep one current avatar projection per participant in local WebMeet state (`roomAvatarsByParticipantId`) and update that projection only through explicit avatar publications, data-channel updates, or participant-attribute avatar changes; routine room resyncs and meeting-detail refreshes may refresh other participant metadata, but they must not recalculate the avatar from older parallel copies such as `session.participant.profileAvatar` or `state.participants[].profileAvatar`. Guests publish the same effective projection through the guest-scoped public-service route for their current joined participant identity, without gaining authenticated workspace user attributes. Remote participant cards render only the current LiveKit projection while connected; they must not call Explorer's protected avatar settings service for another user id. Unresolved workspace users and guests fall back to a first-letter avatar. Card presenters do not fetch DPU data directly and only render the avatar state supplied by the participant layout controller.

Redis supports LiveKit internal state; it is not the user-facing directory.

### WebMeet Event Contract

WebMeet event transport uses one canonical encoded event string:

```text
room:type:base64url_payload
```

The `room` segment is the meeting id for meeting-scoped events and the workspace id for workspace-scoped events. The `type` segment is one of the central `WEBMEET_EVENT_TYPES` values. The payload segment is UTF-8 JSON encoded as base64url and must contain the event `id`, `createdAt`, and the event-specific fields such as `meetingId`, `workspaceId`, `participantId`, `message`, `segmentId`, `recordingId`, or `agentId`.

`webmeet-events.js` is the sole event contract module for the store and browser UI. It defines `WEBMEET_EVENT_TYPES`, payload validators, event builders/parsers, and the persistence flags for every type. Event callers must not write ad-hoc JSON event objects. Persistent meeting and workspace event logs store the encoded event string directly in `.event` files; obsolete JSON event files are not part of the current contract.

Persistent event types include room creation/rename, participant membership changes, stored avatar updates, profile avatar updates, persisted chat/transcript notifications, AI dispatch metadata, recording events, and artifact creation. Realtime-only event types include LiveKit data-channel chat delivery, live avatar projection, and avatar state requests. If a type is realtime-only, it must not be written to meeting or workspace event logs.

### LiveKit Join And SFU Processing

When a WebMeet user joins a room:

1. The browser calls `webmeet_meeting_join` or the guest join API through Ploinky.
2. `webmeetAgent` updates durable meeting presence, records authenticated workspace user ids on participant entries, and returns `livekitUrl`, `roomName`, and a LiveKit participant JWT.
3. The browser opens a WebSocket signaling connection to `livekitUrl`.
4. LiveKit validates the JWT against its configured API key/secret and checks room grants.
5. The browser and LiveKit negotiate WebRTC transport through ICE.
6. Media starts only when the user enables microphone, camera, or screen share.

LiveKit is an SFU. It terminates each browser's WebRTC connection, receives RTP/RTCP packets for published audio/video/screen tracks, tracks subscriber interest, forwards selected encoded streams, handles congestion feedback and retransmission requests, and re-encrypts media for each receiver. It normally forwards browser-encoded media rather than transcoding room media. The egress worker is the component that subscribes to a room, renders/composites, encodes MP4, and writes recording files.

LiveKit does not run room-wide server-side denoise for human participants in this deployment. Human voice cleanup happens before publish in the browser microphone pipeline. Transcript and AI ingestion can receive additional self-hosted denoise in `webmeetStt` before Faster-Whisper. This keeps the LiveKit server as the SFU, avoids paid/cloud media processing, and makes audio-processing ownership explicit.

`webmeetAgent` signs LiveKit participant tokens with `WEBMEET_LIVEKIT_API_SECRET`. Tokens are scoped to one LiveKit room and grant room join, publish, subscribe, and data-channel permissions. API key/secret values are server-side only and must be generated from the shared derived-master labels so `webmeetAgent`, LiveKit server, and egress agree.

### WebRTC Transport Model

WebRTC is a long-lived real-time media session, not a normal HTTP request/response flow.

| Topic | HTTP | WebRTC |
|---|---|---|
| Shape | Request/response | Long-lived media session |
| Best for | Pages, JSON, files, backend actions | Audio, video, screen share, real-time data |
| Latency target | Normal web latency | Low latency |
| Transport behavior | Reliable and ordered | Real-time; late media packets may be dropped |
| Common transport | TCP/TLS, HTTP/2, HTTP/3 | UDP when possible, with TCP/TURN fallback |
| Browser API | `fetch`, forms, WebSocket, EventSource | `RTCPeerConnection`, media tracks, ICE |

The WebMeet plugin uses the LiveKit browser client, so most low-level WebRTC behavior is hidden behind LiveKit APIs. Relevant WebRTC pieces are ICE, STUN, TURN, SRTP, `getUserMedia`, `getDisplayMedia`, and `RTCPeerConnection`.

Media is encrypted in transit on the browser-to-LiveKit and LiveKit-to-browser WebRTC hops. End-to-end media encryption is not configured in this codebase, so LiveKit is trusted infrastructure: it terminates the WebRTC transports, can access encoded media/data-channel payloads required for SFU routing, and then re-encrypts traffic for subscribers.

Browser-facing LiveKit signaling/API traffic is encrypted in transit only when `WEBMEET_PUBLIC_LIVEKIT_URL` uses `wss://` and the deployment terminates TLS. Internal server-side API calls go through `WEBMEET_LIVEKIT_URL`, which must match the active LiveKit topology: `http://liveKitServerAgent:7880` in `default`, `http://liveKitServerAgent:17880` in `dev`, and `http://host.containers.internal:7880` in the host-network production topology.

### WebMeet Browser Room Options

The WebMeet plugin creates a LiveKit `Room` with:

- `adaptiveStream: false`.
- `dynacast: false`.
- `autoSubscribe: true` at connect time.
- explicit subscription sweeps after connect and publication events.
- optional `rtcConfig` built from `session.rtcConfig.iceServers`.

Adaptive media optimizations stay disabled because the modal renders media elements after room events and needs remote video and screen-share downtracks to be negotiated immediately after subscription. Already-attached tracks must not be toggled off just because the browser reports a remote media track as muted before the first frame arrives.

Remote video rendering must include bounded recovery for subscribed video publications whose attached media element remains without decoded frames after the initial readiness window. Recovery may detach the stale element and toggle the LiveKit publication subscription off and back on, but it must be limited per publication so a media-plane fault cannot create an endless subscription loop. Recovery diagnostics must summarize participant, publication, track, and element readiness without exposing tokens, SDP, ICE credentials, API keys, authorization values, or raw LiveKit payloads.

With `WEBMEET_ICE_TRANSPORT_POLICY=all`, TURN remains a fallback and clients may still choose direct LiveKit media candidates when those work. Relay-only mode is a diagnostic or controlled-network policy, not the default.

### Screen Share Flow

Screen sharing is a browser media flow, not an MCP stream. The user clicks the screen-share button, the browser prompts for a screen/window/tab, the LiveKit client publishes a `ScreenShare` video track, LiveKit forwards subscribed packets to remote browsers, and egress subscribes to that track only when recording is active.

Camera and screen share may be active at the same time. They are separate LiveKit video tracks and can render as multiple media elements in the same participant card.

The current WebMeet media controller:

- applies camera capture and publish quality from `cameraQuality`.
- applies screen-share capture and publish quality from `screenShareQuality`.
- publishes screen share with `simulcast: false`.
- captures microphone audio through one of three voice-processing modes:
  - `enhanced`: browser WebRTC audio constraints, high-pass filter, locally bundled `@jitsi/rnnoise-wasm@0.2.1` sync build for `AudioWorklet`, optional 50/60 Hz hum notch, compressor/limiter, and microphone gain before publishing the processed track to LiveKit.
  - `standard`: browser WebRTC echo cancellation/noise suppression/auto gain constraints plus optional local audio graph steps when gain or hum filtering are requested.
  - `off`: no browser echo cancellation or noise suppression, with only explicitly requested local graph steps such as gain or hum filtering.
- supports local deafen, which mutes remote playback in the current browser, disables the local microphone when enabled, and restores the microphone on undeafen only when it was enabled before deafen.
- lets users choose microphone, camera, speaker, voice-processing mode, hum filter, microphone gain, speaker volume, audio processing flags, camera quality, and screen-share quality from the media settings panel.
- shows a short inline explanation for each media and background setting through an info control that becomes visible on hover and on focus/tap, so users can understand the effect of each setting without leaving the panel.
- defaults new browser-scoped audio settings to `standard` voice processing, browser echo cancellation on, browser noise suppression on, auto gain control off, microphone gain at `1.0`, hum filter off, and speaker volume at `0.8` to reduce feedback and room-bleed risk on first join.
- blocks the full WebMeet modal with a room-transition overlay while connecting, disconnecting, or switching rooms so users cannot trigger conflicting actions mid-transition; the overlay must show a dynamic status such as `Connecting to <room name>...` or `Disconnecting from <room name>...`.
- stops local microphone, camera, and screen-share tracks and mutes/unsubscribes remote playback before the application leave request when a user exits a room.
- emits media diagnostics only when an explicit debug flag such as `WEBMEET_MEDIA_DEBUG` or `webmeetMediaDebug=1` is enabled.

Enhanced mode must fall back visibly to `standard` when the browser cannot run `AudioWorklet`/RNNoise. The fallback is an application decision made by the WebMeet media controller; it is not an implicit LiveKit server feature.

The RNNoise vendor copy must remain local to `IDE-plugins/webmeet-tool-button/vendor/rnnoise`, with `THIRD_PARTY_NOTICES.md`, package metadata, and license included. Runtime CDN fetches for RNNoise are not allowed. The Jitsi sync build is used because WebMeet runs RNNoise inside `AudioWorklet`.

Diagnostics must summarize room, track, publication, candidate, and video-element state without logging tokens, SDP blobs, ICE credentials, API keys, or authorization values.

### Redis Boundary

Redis is runtime infrastructure state for LiveKit and Egress. It may contain short-lived LiveKit room, participant, node, routing, and Egress coordination data. Because the supervised Redis process inside `liveKitServerAgent` runs `redis-server --save 60 1`, Redis may snapshot that runtime state to disk under `.ploinky/data/webmeet/redis`.

Redis must not be treated as the source of truth for:

- WebMeet room lists or titles.
- guest invite tokens.
- chat history.
- transcript segments.
- AI artifacts, tasks, decisions, or observer summaries.
- recording artifact metadata.
- media packets or MP4 recordings.

Durable WebMeet records live under `.ploinky/data/webmeetAgent/data`, and recording files live under `.ploinky/data/webmeet/recordings`.

### Recording And Egress Boundary

The browser does not directly control egress. Recording starts and stops through WebMeet tools:

1. Browser calls `webmeet_recording_start` or `webmeet_recording_stop`.
2. `webmeetAgent` calls LiveKit's egress API with a short-lived server JWT granting recording permissions.
3. LiveKit assigns egress through the LiveKit/Redis control plane.
4. Egress subscribes to room media, composites/renders, encodes MP4, and writes under `/data/recordings`.
5. `webmeetAgent` persists recording artifact metadata.

`WEBMEET_EGRESS_URL` can appear in recording metadata, but actual egress control calls use `WEBMEET_LIVEKIT_URL` and LiveKit's `/twirp/livekit.Egress/*` API.

Recording files are not encrypted by the WebMeet meeting-payload encryption layer. Access control depends on workspace filesystem/container exposure and the WebMeet artifact metadata path.

### Security Boundaries

WebMeet uses different controls for application, media, recording, Redis, TURN, and durable data boundaries:

| Boundary | Protection | Limits |
|---|---|---|
| Browser to WebMeet tools | Authenticated Ploinky router and MCP proxy mint invocation metadata. | Protects application tools, not direct LiveKit media sockets. |
| Guest invite HTTP service | Manifest-declared `/public-services/webmeet/...` route with scoped forced guest identity and a router-issued invocation token. | Guest users must not reach Explorer, generic MCP routes, or non-guest WebMeet APIs. |
| LiveKit participant access | Room-scoped LiveKit JWT signed by `WEBMEET_LIVEKIT_API_SECRET`. | Anyone who obtains a participant token can use its grants until expiry. |
| LiveKit API and signaling | API key/secret stay server-side; public signaling should use TLS. | The signaling port is also the API surface; protect it with strong derived keys, firewalling, and TLS termination. |
| LiveKit media transport | WebRTC encrypts media hops. | LiveKit is trusted with encoded media because E2EE is not configured. |
| Redis | Bridge-network runtime service with the host port published so host-network LiveKit can reach it via `127.0.0.1:6379`. | Redis has no product-data role and must not be publicly exposed beyond the deployment host loopback. |
| TURN/STUN | Long-term coturn credentials, returned as ICE servers when configured. | TURN credentials are static; relay traffic can become a bandwidth bottleneck. |
| Durable meeting data | AES-256-GCM meeting payloads with per-meeting DEKs wrapped by `PLOINKY_WEBMEET_MASTER_KEY`. | Metadata outside encrypted payloads remains visible; recordings are file artifacts outside payload encryption. |

`PLOINKY_WEBMEET_MASTER_KEY` is a WebMeet data-encryption key derived from `PLOINKY_DERIVED_MASTER_KEY` through manifest `derive: "derived-master"`. New payloads must not use `PLOINKY_MASTER_KEY` or `PLOINKY_DERIVED_MASTER_KEY` directly. A legacy decrypt-only path may exist only to migrate older records into the dedicated WebMeet key.

Logs and browser diagnostics must not expose secrets, cookies, bearer tokens, invocation JWTs, LiveKit participant JWTs, SDP, ICE credentials, API keys, raw prompts, or hidden payloads.

### Public Deployment And Cloudflare/Nginx Topology

`WEBMEET_PUBLIC_LIVEKIT_URL` is the browser-facing LiveKit signaling URL. It must be reachable from the browser and should be `wss://` for public deployments.

`WEBMEET_LIVEKIT_URL` is the server-side LiveKit API URL used by `webmeetAgent` for Egress control. It must remain internal to the deployment host. With LiveKit on `network.mode: "host"` in production, the supported value for bridge-resident `webmeetAgent` is `http://host.containers.internal:7880`; with LiveKit on the `webmeet` bridge in `default` and `dev`, the supported value is the `liveKitServerAgent` alias on the profile's signaling port (`:7880` default, `:17880` dev).

The production profile declares non-sensitive WebMeet topology defaults for the Axiologic deployment (`wss://livekit-skills.axiologic.dev`, `http://host.containers.internal:7880`, host-gateway Egress URL, TURN realm, and TURN hostname). A fresh production workspace can therefore start from the profile alone. Operators may still override those defaults with Ploinky vars when deploying the same agents behind another hostname or when coturn needs an explicit external IP.

Important production variables:

| Variable | Contract |
|---|---|
| `WEBMEET_PUBLIC_LIVEKIT_URL` | Browser-reachable `ws://` or `wss://` LiveKit signaling URL. Prod profile default: `wss://livekit-skills.axiologic.dev`. |
| `WEBMEET_LIVEKIT_URL` | Server-side LiveKit API URL for `webmeetAgent`. Use `http://liveKitServerAgent:7880` in `default`, `http://liveKitServerAgent:17880` in `dev`, and `http://host.containers.internal:7880` with host-network LiveKit in `prod`. |
| `WEBMEET_LIVEKIT_INTERNAL_WS_URL` | Internal WS URL the supervised Egress uses to reach LiveKit. Default is `ws://127.0.0.1:7880` (`:17880` in `dev`), since LiveKit and Egress share a container. |
| `WEBMEET_LIVEKIT_API_KEY` and `WEBMEET_LIVEKIT_API_SECRET` | Derived shared LiveKit credentials used by token signing and LiveKit server config. |
| `WEBMEET_INFRA_IMAGE_TAG` | `liveKitServerAgent` Docker image tag. Default declared in manifest (`webmeet-infra`). Override via repo var or workflow input. |
| `WEBMEET_LIVEKIT_USE_EXTERNAL_IP` | Controls LiveKit external IP discovery. |
| `WEBMEET_LIVEKIT_NODE_IP` | Explicit public node IP when external discovery is disabled. |
| `WEBMEET_TURN_HOST` | Browser-facing TURN hostname and coturn hostname-to-IP resolution source. Prod profile default: `livekit-skills.axiologic.dev`. |
| `WEBMEET_TURN_EXTERNAL_IP` | Optional explicit coturn external IP override. In prod, the manifest default is `auto`, which resolves `WEBMEET_TURN_HOST` at startup. |
| `WEBMEET_LIVEKIT_FORCE_TCP` | Forces LiveKit media over TCP when UDP is unavailable. Keep `false` when UDP media ports are directly reachable; documented rollback knob if a future regression breaks UDP again. |
| `WEBMEET_LIVEKIT_LOG_LEVEL` | LiveKit log verbosity. Use `debug` only temporarily and reset to `info`. |
| `WEBMEET_LIVEKIT_REDIS_ADDRESS` | Redis address written into LiveKit's `redis.address`. Defaults to `127.0.0.1:6379` in every profile because Redis runs as a supervised service inside `liveKitServerAgent`. |
| `WEBMEET_ICE_TRANSPORT_POLICY` | Browser ICE policy. Defaults to `all`; `relay` is a controlled diagnostic or network policy. |
| `WEBMEET_TURN_*` | Coturn external address, realm, user, password, and relay port range returned as ICE servers when configured. |

Cloudflare Tunnel can carry LiveKit HTTPS/WebSocket signaling, but public Tunnel hostnames are not a general UDP proxy for WebRTC media. `livekit-skills.axiologic.dev` is fronted by the Nginx TLS terminator and Certbot renewal loop supervised by `liveKitServerAgent` in the `prod` profile (see `webmeetInfra/docs/specs/DS002`). The reverse proxy is responsible only for HTTPS/WebSocket signaling to LiveKit `7880/tcp`. WebRTC media must reach the LiveKit host candidates directly on `7882-7892/udp`, with `7881/tcp` left open as fallback. LiveKit's signaling listener cannot terminate TLS itself in `livekit-server 1.11`; the only TLS-aware options on that binary are `--turn-cert`/`--turn-key` for the TURN server, so a TLS terminator in front of `7880` remains required.

For the direct-UDP topology, Cloudflare DNS for the LiveKit hostname must be a DNS-only `A` record to the LiveKit host rather than a Cloudflare Tunnel public hostname. `skills.axiologic.dev` may remain behind Cloudflare Tunnel because it carries the Explorer application plane, not LiveKit UDP media. The `cloudflared` connector that serves `skills.axiologic.dev` runs as a plain podman container with `--network=host` and `RestartPolicy=always` rather than as a Ploinky agent; this is a deliberate exception to keep the public ingress decoupled from the Ploinky agent lifecycle so deploys do not interrupt traffic to the dashboard.

### Capacity And Bottlenecks

The current deployment is a single LiveKit server, one Redis instance, one coturn container, and one egress worker. That is adequate for small rooms but has predictable scaling pressure:

| Component | Bottleneck risk | Signals |
|---|---|---|
| LiveKit network egress | High; outbound media grows with subscribers and visible tracks. | NIC throughput, packet loss, high RTT, lower selected video layers. |
| LiveKit CPU | Medium to high; handles WebRTC encryption, RTCP feedback, retransmission, and per-peer state. | Container CPU, reconnects, scheduler latency, LiveKit warnings. |
| LiveKit UDP media range | Medium; `7882-7892/udp` and `17882-17892/udp` are narrow for high concurrency. | Signaling succeeds but new participants fail ICE/connect. |
| Browser CPU/GPU | Medium; clients decode and render remote streams. | Dropped frames, UI jank, thermal throttling. |
| Egress CPU | High during recordings; recording composites and encodes MP4. | Slow recording start, failed egress jobs, high egress CPU/memory. |
| Recording storage | Medium; long recordings write large files. | Disk usage, write latency, incomplete files. |
| Coturn | High when relay is used; relay doubles media bandwidth through TURN. | Relay allocation count and TURN bandwidth. |
| Redis | Low to medium; not on the media packet path. | Redis latency, reconnect logs, high room churn. |
| `webmeetAgent` subprocesses | Low for media, medium for chat/AI bursts. | MCP latency, job backlog, file write errors. |

For a rough mental model, each participant uploads local tracks once to LiveKit, while LiveKit may forward many remote tracks back to each subscribed participant. Aggregate LiveKit outbound traffic can therefore grow much faster than a single user's uplink.

Scaling options include expanding the UDP media port range, confirming firewall/NAT rules match the range, adding TURN/TLS or TURN/UDP for restrictive networks, moving recording to dedicated egress capacity, monitoring LiveKit and egress separately, and adopting LiveKit's multi-node Redis-backed pattern for larger deployments.

### Operational Checks

After startup or deployment, useful checks include:

```sh
ploinky status
podman ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
podman exec <webmeetAgent-container> node /code/server/validate-runtime.mjs
```

Expected WebMeet containers:

- `liveKitServerAgent` — supervises Redis, Coturn, LiveKit Server, LiveKit Egress, plus Nginx and Certbot in the `prod` profile
- `webmeetAgent`

If LiveKit or Egress fails to start, inspect the generated config under:

- `.ploinky/agents/liveKitServerAgent/livekit.yaml`
- `.ploinky/agents/liveKitServerAgent/egress.yaml`
- `.ploinky/agents/liveKitServerAgent/redis.conf`
- `.ploinky/agents/liveKitServerAgent/turnserver.conf`

If the UI lists rooms but media does not connect, verify:

- `webmeet_meeting_join` returns a non-empty `participantToken`.
- `livekitUrl` is reachable from the browser.
- LiveKit media ports are published and reachable for the active profile.
- Browser candidate stats select the expected transport (`udp` for direct media, `tcp` only when forced or as fallback).
- TURN configuration is reachable and supplied to the browser when relay behavior is required.

## Decisions & Questions

### Question #1: Why is LiveKit not treated as the WebMeet application database?

Response:
LiveKit is the media SFU. It owns real-time room signaling and track state for the media session, but it does not own WebMeet room lists, guest invite tokens, chat history, transcripts, artifacts, or AI jobs. Those records live in the WebMeet store so application authorization, encryption, and workspace scoping remain under `webmeetAgent`.

### Question #2: Why is direct UDP required for the preferred production media path?

Response:
WebRTC prefers UDP because real-time media should keep moving instead of waiting for late packets. Cloudflare Tunnel can proxy the LiveKit WebSocket signaling path, but it is not a public UDP media proxy. If the LiveKit hostname is a Tunnel public hostname and UDP media is not directly reachable, signaling can succeed while subscribers fail to receive usable keyframes. In that topology `WEBMEET_LIVEKIT_FORCE_TCP=true` is a workaround, but the preferred low-latency production path is direct UDP media to LiveKit plus TCP/TURN fallback.

### Question #3: Why disable adaptive stream and dynacast?

Response:
The WebMeet modal creates and attaches video elements after LiveKit room events. Immediate subscription and downtrack negotiation is more important than SDK visibility optimization in the current UI, especially for screen share. If future UI changes make media elements stable earlier, adaptive behavior can be re-evaluated with browser tests.

## Conclusion

WebMeet must keep its control plane, media plane, recording plane, runtime state, and durable application state separate. `webmeetAgent` owns application state and authorization; LiveKit owns real-time media routing; egress owns recording work; Redis is transient LiveKit coordination; coturn is a relay fallback. Public deployments must expose LiveKit signaling and media according to WebRTC requirements, keep generated secrets derived from `PLOINKY_DERIVED_MASTER_KEY`, and validate changes with browser media tests that inspect decoded frames and selected ICE candidates.
