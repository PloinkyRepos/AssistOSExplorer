async function loadPartial(selector, path) { const target = document.querySelector(selector); if (!target) return; const response = await fetch(path, { cache: 'no-cache' }); if (response.ok) target.innerHTML = await response.text(); }
function initializeMenus() { document.querySelectorAll('.site-nav__trigger').forEach((trigger) => trigger.addEventListener('click', () => { const group = trigger.closest('.site-nav__group'); const menu = group.querySelector('.site-nav__menu'); const open = trigger.getAttribute('aria-expanded') === 'true'; document.querySelectorAll('.site-nav__menu').forEach((item) => item.setAttribute('hidden', '')); document.querySelectorAll('.site-nav__trigger').forEach((item) => item.setAttribute('aria-expanded', 'false')); if (!open) { menu.removeAttribute('hidden'); trigger.setAttribute('aria-expanded', 'true'); } })); document.addEventListener('click', (event) => { if (!event.target.closest('.site-nav__group')) document.querySelectorAll('.site-nav__menu').forEach((item) => item.setAttribute('hidden', '')); }); }
document.addEventListener('DOMContentLoaded', async () => { await Promise.all([loadPartial('#site-header', 'partials/header.html'), loadPartial('#site-footer', 'partials/footer.html')]); (await import('../../docs/documentation-breadcrumb.js')).initializeDocumentationBreadcrumb('WebMeet STT'); initializeMenus(); });
